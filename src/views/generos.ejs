<!DOCTYPE html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Generos</title>
	</head>
	<body>
		<h1>Generos</h1>

		<% if (generos.length > 0) { %>
		<table border="1">
			<thead>
				<tr>
					<th>ID</th>
					<th>Nombre</th>
				</tr>
			</thead>
			<tbody>
				<% generos.forEach(item => { %>
				<tr>
					<td><%= item.idGenero %></td>
					<td><%= item.nombreGenero %></td>
				</tr>
				<% }); %>
			</tbody>
		</table>
		<% } else { %>
		<p>No se encontraron datos de los generos.</p>
		<% } %>
	</body>
</html>
